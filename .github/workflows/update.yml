name: Update ΑΣΕΠ Feed (DISABLED)

# Αυτό το workflow δεν χρησιμοποιείται πλέον.
# Ο scraper τρέχει τοπικά στον υπολογιστή μέσω Task Scheduler.

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Ο scraper τρέχει τοπικά στον υπολογιστή."
